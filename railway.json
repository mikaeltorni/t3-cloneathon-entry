{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd web-app && npm install && cd .. && npm run build"
    },
    "deploy": {
      "preDeployCommand": "npm install && cd web-app && npm install",
      "startCommand": "npm run start"
    }
}